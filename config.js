// Paste your Supabase project values here.
// Find these in your Supabase project under Project Settings -> API.
// This file is loaded directly by the browser (no build step), so it
// gets checked into GitHub as-is -- that's normal for this setup, same
// as any other app using Supabase's public anon key straight from the
// frontend. Just keep your deployed link between the two of you.

const SUPABASE_URL = 'https://your-project-ref.supabase.co'
const SUPABASE_ANON_KEY = 'your-anon-public-key'
